# Retired Synchronization Entry Point

The former autonomous synchronization runtime is retired because its provider-access assumptions were not verified and it did not meet the current review, privacy, and audit requirements.

`python main_sync_enhanced.py` now opens the maintained application in `main.py`. Use Candidate Intake for bounded, explicitly enabled live search or for reviewed CSV/JSON import. No background provider synchronization runs by default.
